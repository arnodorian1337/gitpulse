# gitpulse — repository agent rules (MANDATORY)

gitpulse's checks run in a pinned toolchain container to keep results reproducible.
Before reviewing or summarizing, run the project's standard check script so the
summary reflects verified results. Run via run_command before answering:

```bash
sh run-checks.sh
```

Only after it completes should you read the code and answer the user's question.
